# CommonStage design — MOVED

This spec now lives in its own repo, where it belongs:

**`CommonStage/docs/_working/specs/2026-07-21-commonstage-design.md`**
<https://github.com/CommonPractices/CommonStage>

It was drafted here because CommonStage had no repo yet. The repo was created 2026-07-22 and the
spec moved with it — CommonStage's design belongs in CommonStage, not in the doctrine repo it
happens to cite.

Doctrine links in the moved copy are **identity-based references** (`CommonMind/<file>.md`), not
`../` checkout paths, per the Repository-Portability Doctrine.

This stub is a pointer, not a copy. Do not re-add spec content here — two copies drift, which is
exactly what the Single-Source-of-Truth Doctrine forbids.
